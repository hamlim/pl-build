import React from 'react'
import Button from 'button'
import PropTypes from 'prop-types'

const ComplexDemo = () => (
  <Button onClick={() => alert('Interaction')}>
    Complex Demo
  </Button>
)

export default ComplexDemo
